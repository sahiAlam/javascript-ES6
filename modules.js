// 1.An ES6 module is a JavaScript file that executes in strict mode only. It means that any variables or functions declared in the module won't be added automatically to the global scope.
// 2.Modules are basic containers in Node/JavaScript system. 1 file can be one module in Javascript.
// # Modules provide a way to organize code into logical units and make it reusable across
// your application or project. They help you break down large applications, libraries, and frameworks into smaller manage

import { myAge } from "./modules_second";
console.log(myAge);
